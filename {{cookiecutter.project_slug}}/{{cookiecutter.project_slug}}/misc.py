def append_phrase(string: str):
    """
    Appends a fixed phrase to the end of a string

    Args:
        string: the string the phrase will be appended to

    Returns:
        A string where a fixed phrase was appended

    """

    return f"{string}...with fries?"
